source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local preview behaves like production.
# Only needed for local preview: GitHub Pages builds the site without it.
#   bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
